# 📚 Що нового я навчився під час виконання цього домашнього завдання

## 🎯 Загальний огляд

Під час виконання цього домашнього завдання я отримав глибоке практичне розуміння роботи з рекурентними нейронними мережами (LSTM) для прогнозування часових рядів. Це було комплексне дослідження, яке охопило всі етапи машинного навчання - від підготовки даних до аналізу результатів.

---

## 🧠 1. Глибоке розуміння архітектури LSTM

### Що навчився:

**Архітектура та принцип роботи:**
- Зрозумів, як LSTM вирішує проблему "vanishing gradient", яка притаманна звичайним RNN
- Дізнався про внутрішню структуру LSTM комірок: forget gate, input gate, output gate та cell state
- Усвідомив, чому LSTM ефективні для часових рядів - вони можуть запам'ятовувати довгострокові залежності

**Практична реалізація:**
```python
self.lstm = nn.LSTM(
    input_size=1,
    hidden_size=hidden_size,
    num_layers=num_layers,
    batch_first=True
)
```

**Ключове усвідомлення:** LSTM не просто "дивиться" на попередні значення, а "запам'ятовує" важливі паттерни через механізм cell state, що робить їх потужними для складних часових залежностей.

---

## 📊 2. Особливості роботи з часовими рядами

### Що навчився:

**Критична важливість хронологічного порядку:**
- Навчився, що для часових рядів **НЕМОЖНА** використовувати випадкове розділення на train/test
- Зрозумів концепцію "витоку інформації з майбутнього" (data leakage)
- Усвідомив, що тренувальні дані завжди мають бути давнішими, ніж тестові

**Приклад правильного розділення:**
```python
train_size = int(len(timeseries) * 0.67)
train = timeseries[:train_size]  # Старіші дані
test = timeseries[train_size:]   # Новіші дані
```

**Чому це важливо:** В реальному житті ми завжди прогнозуємо майбутнє на основі минулого, тому навчальний процес має відображати цю реальність.

---

## 🔄 3. Sliding Window підхід

### Що навчився:

**Концепція вікна:**
- Навчився перетворювати одномірний часовий ряд у структуровані дані для навчання
- Зрозумів параметр `lookback` - скільки попередніх значень використовувати для прогнозування
- Усвідомив, що більший lookback може покращити якість, але збільшує складність

**Практична реалізація:**
```python
for i in range(len(dataset) - lookback):
    X.append(dataset[i:(i + lookback)])  # Вікно попередніх значень
    y.append(dataset[i + lookback])      # Наступне значення
```

**Важлива деталь:** При lookback=1 ми використовуємо лише одне попереднє значення, що є базовим підходом. Експериментування з lookback=3,6,12 може значно покращити результати.

---

## 🎨 4. Важливість форматів тензорів PyTorch

### Що навчився:

**Розмірності для LSTM:**
- Навчився, що LSTM очікує тензор формату `(batch_size, sequence_length, input_size)`
- Зрозумів параметр `batch_first=True` та його важливість
- Усвідомив необхідність `unsqueeze(-1)` для додавання розмірності features

**Практичний приклад:**
```python
X = torch.tensor(X, dtype=torch.float32)
X = X.unsqueeze(-1)  # Додаємо розмірність features
# Результат: (95, 1, 1) = (samples, lookback, features)
```

**Типова помилка:** Забування про третю розмірність призводить до помилки "Expected 3D tensor, got 2D".

---

## ⚙️ 5. Оптимізація та навчання нейронних мереж

### Що навчився:

**Adam оптимізатор:**
- Зрозумів, чому Adam краще за стандартний SGD для LSTM
- Дізнався, що Adam автоматично налаштовує learning rate для кожного параметра
- Усвідомив важливість `optimizer.zero_grad()` перед кожним backward pass

**Цикл навчання:**
```python
# Прямий прохід
predictions = model(batch_x)
loss = criterion(predictions, batch_y)

# Зворотний прохід
optimizer.zero_grad()
loss.backward()
optimizer.step()
```

**Ключове усвідомлення:** Без `zero_grad()` градієнти акумулюються, що призводить до некоректного навчання.

---

## 📉 6. Моніторинг та діагностика навчання

### Що навчився:

**Важливість валідації:**
- Навчився відслідковувати train та test RMSE для виявлення перенавчання
- Зрозумів, що великий розрив між train та test RMSE сигналізує про overfitting
- Усвідомив необхідність `model.eval()` та `torch.no_grad()` під час валідації

**Візуалізація процесу:**
- Графік loss показує, чи навчається модель
- Порівняння прогнозів з реальними даними дає інтуїтивне розуміння якості
- Швидке зменшення loss на початку - це нормально

**Практичний досвід:** Якщо loss не зменшується - проблема може бути в learning rate, архітектурі або даних.

---

## 🔧 7. Вплив гіперпараметрів на якість моделі

### Що навчився:

**hidden_size:**
- Експериментував з hidden_size=50 та hidden_size=100
- Зрозумів, що більший hidden_size дає більшу "пам'ять" моделі
- Усвідомив ризик перенавчання при надто великому hidden_size на малих даних

**Інші важливі параметри:**
- `batch_size=8` - компроміс між швидкістю та стабільністю навчання
- `num_epochs=2000` - достатньо для збіжності
- `lookback=1` - базовий підхід, але може бути недостатнім

**Висновок:** Немає "ідеальних" гіперпараметрів - потрібне експериментування та аналіз результатів.

---

## 💾 8. Оптимізація пам'яті та обчислень

### Що навчився:

**float32 vs float64:**
- Дізнався, що float32 вдвічі економить пам'ять порівняно з float64
- Зрозумів, що для більшості задач точності float32 достатньо
- Усвідомив важливість оптимізації при роботі з великими датасетами

**Практичне застосування:**
```python
timeseries = df['Passengers'].values.astype(np.float32)
X = torch.tensor(X, dtype=torch.float32)
```

**Економія:** На 144 спостереженнях різниця непомітна, але на мільйонах - критична.

---

## 📊 9. Метрики оцінки якості

### Що навчився:

**MSE vs RMSE:**
- MSE (Mean Squared Error) - функція втрат під час навчання
- RMSE (Root MSE) - зручніша для інтерпретації (в тих же одиницях, що й дані)
- Зрозумів, чому RMSE=100 означає помилку в 100,000 пасажирів для цих даних

**Інтерпретація результатів:**
- RMSE треба порівнювати з діапазоном значень у даних
- Якщо RMSE=30 при значеннях 100-600, це ~5-30% помилка
- Важливо дивитися не лише на число, а й на візуалізацію прогнозів

---

## 🎓 10. DataLoader та батч-обробка

### Що навчився:

**Чому потрібен DataLoader:**
- Автоматичне розбиття на батчі
- Перемішування даних перед кожною епохою
- Ефективне використання пам'яті

**Конфігурація:**
```python
train_loader = DataLoader(
    train_dataset,
    batch_size=8,
    shuffle=True
)
```

**Розуміння:** DataLoader не просто зручність - це критичний компонент для навчання на великих даних.

---

## 🔍 11. Виявлення та діагностика проблем

### Що навчився:

**Типові проблеми та їх вирішення:**

1. **Модель не навчається (loss не зменшується):**
   - Перевірити learning rate
   - Перевірити архітектуру моделі
   - Переконатися, що дані правильно нормалізовані

2. **Перенавчання (train loss << test loss):**
   - Зменшити складність моделі
   - Додати dropout
   - Збільшити обсяг даних

3. **Нестабільне навчання:**
   - Зменшити learning rate
   - Збільшити batch_size
   - Використати gradient clipping

---

## 🚀 12. Практичні навички роботі з PyTorch

### Що навчився:

**Основи PyTorch:**
- Створення класів моделей через `nn.Module`
- Робота з `forward` методом
- Управління режимами `train()` та `eval()`

**Робота з тензорами:**
- Створення тензорів різних типів
- Операції з розмірностями (unsqueeze, squeeze, reshape)
- Переміщення між CPU та GPU (хоча в цьому проекті використовувався CPU)

**Best practices:**
- Завжди викликати `model.eval()` перед валідацією
- Використовувати `torch.no_grad()` для економії пам'яті
- Правильно структурувати код (класи, функції)

---

## 💡 13. Візуалізація та інтерпретація результатів

### Що навчився:

**Важливість візуалізації:**
- Графік loss допомагає зрозуміти, чи навчається модель
- Порівняння прогнозів з реальними даними показує, де модель помиляється
- Візуалізація допомагає знайти паттерни та аномалії

**Техніка зсуву прогнозів:**
```python
train_plot = np.ones_like(timeseries) * np.nan
train_plot[lookback:train_size] = train_pred.squeeze().numpy()
```

**Розуміння:** NaN використовується для коректного відображення прогнозів на графіку.

---

## 🎯 14. Порівняльний аналіз моделей

### Що навчився:

**Методика порівняння:**
- Тренування декількох моделей з різними параметрами
- Об'єктивне порівняння за метриками (train/test RMSE)
- Візуальне порівняння графіків loss

**Висновки з експерименту:**
- Модель з hidden_size=100 зазвичай краща на train
- Але ризик перенавчання вищий
- Потрібен баланс між складністю та узагальненням

---

## 🌟 15. Розуміння обмежень та напрямків покращення

### Що навчився:

**Поточні обмеження:**
- Малий обсяг даних (144 спостереження)
- Простий lookback=1
- Відсутність нормалізації
- Відсутність обробки сезонності

**Потенційні покращення:**
1. Збільшити lookback до 3-12 місяців
2. Застосувати MinMaxScaler для нормалізації
3. Додати dropout для регуляризації
4. Експериментувати з num_layers>1
5. Додати ознаки місяця/кварталу для сезонності

---

## 🎓 Підсумкові висновки

### Найважливіші знання:

1. **LSTM - це потужний інструмент для часових рядів**, але вимагає правильної підготовки даних
2. **Хронологічний порядок критично важливий** для часових рядів
3. **Візуалізація - не менш важлива за метрики** для розуміння моделі
4. **Експериментування з гіперпараметрами** - ключ до покращення якості
5. **Моніторинг навчання** допомагає вчасно виявити проблеми

### Навички, які я розвинув:

✅ Робота з PyTorch та нейронними мережами  
✅ Підготовка та обробка часових рядів  
✅ Проектування архітектури LSTM моделей  
✅ Налаштування процесу навчання та оптимізації  
✅ Аналіз та візуалізація результатів  
✅ Діагностика проблем та покращення моделей  
✅ Порівняльний аналіз різних підходів  

### Практичне застосування:

Ці знання можна застосувати до:
- Прогнозування продажів
- Фінансових часових рядів (ціни акцій, валют)
- Прогнозування погоди
- Аналіз медичних даних (пульс, тиск тощо)
- IoT сенсорів та моніторингу

---

## 🙏 Висновок

Це домашнє завдання дало мені не просто теоретичні знання про LSTM, а **практичний досвід** роботи з повним циклом ML проекту. Я навчився:

- Ставити правильні питання перед початком роботи
- Методично підходити до експериментів
- Аналізувати результати та робити висновки
- Розуміти компроміси між різними підходами
- Бачити напрямки для подальшого покращення

Найважливіше - я зрозумів, що **машинне навчання - це ітеративний процес**, де кожен експеримент дає нові інсайти та ідеї для покращення. Немає "ідеального" рішення з першого разу, але систематичний підхід та аналіз ведуть до все кращих результатів.

---

**Дата завершення:** 1 грудня 2025  
**Проект:** LSTM для прогнозування часових рядів  
**Статус:** Успішно завершено ✅
